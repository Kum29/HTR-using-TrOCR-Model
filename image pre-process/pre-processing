import cv2
import numpy as np

image = Image.open("scan.jpg")
image = np.array(image)  
gray_image = cv2.cvtColor(image, cv2.COLOR_BGR2GRAY)
thresh_value, im_bw = cv2.threshold(enhanced, 0, 255, cv2.THRESH_BINARY + cv2.THRESH_OTSU)
kernel = cv2.getStructuringElement(cv2.MORPH_RECT, (80, 5))
dilated = cv2.dilate(im_bw,kernel,iterations=1)
cv2.imwrite("dilated.jpg", dilated)
display("dilated.jpg")


# till here image has reached a state where it can be processed. A computer can read this binary version of the image easily



